SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL (по умолчанию). Запуск: docker compose up -d
DB_NAME=clinic_network
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Быстрый старт без Docker/PostgreSQL — раскомментируйте:
# USE_SQLITE=True
